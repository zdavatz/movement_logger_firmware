# Build output
/STM32CubeIDE/build/

# Build counter — bumps every `make`, embedded via -DPL_BUILD_NUM.
# Local-only; committing would create spurious diffs.
/STM32CubeIDE/.build_counter

# Finder-induced "Kopie" duplicates — never intended to be committed
*Kopie*
* Kopie*

# macOS
.DS_Store

# ST-copyrighted reference PDFs kept locally under pdf/ for offline use
# but NOT redistributed in this GPLv3 repo (README links ST to obtain
# them). Only our own GPS_SOLDERING.pdf is tracked.
/pdf/steval-mkboxpro-schematic.pdf
/pdf/um3133-sensortilebox-pro.pdf
